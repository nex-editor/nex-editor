{
  "name": "nex-editor-web-playground",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build ../../crates/wasm --target web --out-dir ../../apps/web-playground/src/wasm --out-name nex_editor_wasm",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  }
}
